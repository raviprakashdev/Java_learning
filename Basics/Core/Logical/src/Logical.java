public class Logical {
    public static void main(String args[]){
        boolean hasAccount = true;
        boolean isLogggedIn = true;
        boolean hasPaid = false;

        System.out.println(hasAccount && isLogggedIn && hasPaid);
        System.out.println(hasAccount || isLogggedIn || hasPaid);

    }
}
